- `preproc/register_prestatic_sweep_frames.py`, `preproc/prestatic_registration_template.yaml`, and `tests/test_register_prestatic_sweep_frames.py`: added a resumable derived pre-static controller that keeps a completed sparse-sweep run immutable, selects a strict denser target time grid by canonical source index, reuses seed image/database identities, snapshots SQLite safely, extracts features only for database-missing frames with the existing sweep camera, imports explicit successful-seed/temporal-neighbour pairs, and runs image registration, triangulation, and intrinsics-frozen bundle adjustment. It validates target registration, finite registered poses, camera identity, and base hashes; packages only successfully registered target-grid RGB/masks; recomputes normalization from target-grid cameras; and republishes the unchanged version-1 static dataset, reference-camera, source-manifest, and READY contracts. Focused tests cover the 1467-frame 30-to-10-fps curtain case (489 targets, 49 reused seed frames, and 440 new images), pair coverage including reused-but-unregistered seeds, command construction, SQLite immutability, target-only packaging, and no-write dry runs. No Python tests, compilation, COLMAP, or GPU work was run locally; verification was limited to static review and whitespace/diff checks.
- `run_experiment_pipeline.py` and `tests/test_run_experiment_pipeline.py`: canonicalized `frequency_selection.mode_counts` to a JSON list in the resolved pipeline payload before writing or comparing it. This fixes false immutable-config mismatches on every second controller invocation, while preserving the already-written resolved JSON schema, YAML list contract, config identity, checkpoints, artifacts, and paths; added repeated-controller JSON round-trip regression coverage.
- `preproc/experiment_pipeline_template.yaml` and `tests/test_run_experiment_pipeline.py`: restored the formal experiment template to the established static-training default of 40,000 foreground plus 100,000 background Gaussians. The controller already forwards the explicit `static.num_bg` value through static training and modal checkpoint materialization; mask and depth losses remain disabled, and existing experiment configs, APIs, paths, checkpoints, and artifact formats are unchanged.
- `flow3d/configs.py`: changed the default mask-supervision weight from `w_mask=1.0` to `w_mask=0.0`, so default training no longer applies foreground/background mask-separation loss. Masks remain available for static COLMAP foreground/background initialization, and experiments can still opt into the unchanged loss implementation with an explicit `--loss.w-mask` value. Checkpoint, dataset, and CLI contracts are unchanged; no tests or compilation were run.
- `preproc/run_prestatic_pipeline.py` and `preproc/prestatic_pipeline_template.yaml`: added a strict image-first controller for the pre-static experiment stages. Canonical uint8 RGB and binary-mask PNG sequences are validated without video decoding or any rotate/crop/resize path; fixed-camera references and a time-grid sweep sample are byte-copied into camera-grouped COLMAP folders, reconstructed together, selected by reference completeness and sweep registration, and packaged with only registered sweep RGB/masks into the existing static COLMAP dataset contract. The controller exports unchanged version-1 fixed-reference camera records plus scene normalization, writes immutable source/resolved provenance and a READY report under `data_formal`, and supports completed-stage resume plus content-validated recovery of its own staged/package publications while refusing ambiguous partial COLMAP databases. `tests/test_run_prestatic_pipeline.py` adds focused schema, RGB/mask, sampling, reference, grouping, no-transform staging, registered-only packaging, retry, and dry-run coverage. No Python tests, COLMAP, or GPU work was run locally; verification was limited to static review and whitespace/diff checks.
- `flow3d/modal_rendered_design.py` and `preproc/build_rendered_modal_design.py`: added an immutable foreground-only gsplat projection artifact that renders the complete motion-filled Gaussian `J phi` field at eroded, alpha-valid flow pixels, alpha-normalizes the `[P,2,2K]` design, records strict checkpoint/Gaussian/phi/camera/cache identities, reports rank/conditioning, optionally writes five-class contributor diagnostics, and performs direct-render packing plus full-rank synthetic-coordinate recovery checks. `flow3d/modal_flow_coordinates.py`, `flow3d/modal_physics_coordinates.py`, and their CLIs now generate `rendered_projection_ridge_v1` coordinates from this artifact and re-evaluate physics with the same rendered design, while existing `reference_flow_ridge_v1` artifacts remain load-only compatible and the coordinate NPZ version-1 schema is unchanged. `run_training.py`, `flow3d/renderer.py`, and `run_modal_reconstruction.py` preserve and validate rendered-design provenance in fixed checkpoints. `modal_peak_pick/apps/compare_reconstruction_ui.py` and `run_modal_peak_pick.py` accept the new artifact, use its exact pixels/design, fit one complex view/mode scale against exact raw DFT modes, and retain the legacy topology comparison path for existing results. No local compilation or GPU rasterization was run.
- `flow3d/modal_joint_optimization.py`, `flow3d/modal_canonical_optimization.py`, and `flow3d/modal_bilateral_graph.py`: deleted the inactive joint/phi-only/canonical-only modal-refinement branches and removed their configuration, parameter, training, scene-model, renderer, and reconstruction consumers. Static training and the accepted fixed `per_frame_flow_coordinates_v1` ridge/physics-coordinate pipeline remain; existing fixed/static checkpoint structure is preserved through the all-false `modal_phi_trainable_mask`, while checkpoints containing removed refinement state now fail explicitly.
- Removed thirteen files outside the accepted bush/corn pipeline: eight standalone diagnostic, comparison, manifest-combine, and inactive bilateral-graph entry points; their four single-consumer `flow3d` diagnostic implementations; and the unreferenced mesh extractor. The static 3DGS, greedy frequency selection, rigid/modal solve, flow/physics-coordinate, checkpoint materialization, reconstruction, Viser, artifact, and checkpoint contracts are unchanged; dependency-coupled validation, trajectory, and optional modal-refinement modules remain in place for a separate cleanup plan.
- `modal_peak_pick/apps/compare_reconstruction_ui.py` and `run_modal_peak_pick.py`: added opt-in, identity-keyed persistent caching for the browser reconstruction comparison through `--comparison-cache-dir`. Each visited view atomically stores its candidate pixels, raw spectrum, projected modal modes, and reconstructed spectrum, while each exact clicked frequency is cached separately and reused across process restarts; visited views and exact modes are also retained in memory during one process. Cache identities cover the loaded modal modes/topology/view alpha plus immutable flow-cache metadata and array file state, so changed inputs select a new cache namespace without overwriting older artifacts. Existing single/multi-view commands, manifest/flow/checkpoint contracts, frequency behavior, and rendered UI remain unchanged when the option is omitted.
- `modal_peak_pick/apps/compare_reconstruction_ui.py` and `run_modal_peak_pick.py`: generalized the browser reconstruction comparison from the first manifest view to all explicitly supplied `VIEW_ID=PATH` flow caches, with a manifest-ordered View dropdown above the existing U/V control. Switching views rebuilds that view's candidate spectrum, projected reconstruction, shared-scale modal images, titles, and status while preserving the selected frequency/component; the original single-view `--cache-dir` command remains supported, and all cache, manifest, checkpoint, and output contracts are unchanged.
- `modal_peak_pick/apps/compare_reconstruction_ui.py`: made the nested Gradio spectrum-click callbacks expose concrete `gr.SelectData` annotations to the installed Gradio event inspector instead of postponed strings. Click events now receive their coordinate payload rather than `None`; spectrum mapping, reconstruction, GUI layout, and all artifact contracts are unchanged.
- `modal_peak_pick/apps/compare_reconstruction_ui.py`: made the original and reconstructed spectrum panels use identical horizontal and vertical limits. Their shared power range is recomputed from both visible curves and the current exact/snapped selections, so either panel expands both plots together without changing frequency selection, projection, modal-image rendering, or artifact contracts.
- `modal_peak_pick/apps/compare_reconstruction_ui.py` and `run_modal_peak_pick.py`: added a browser-based Gradio view1 diagnostic with a 2x2 original/reconstructed spectrum and modal-image layout. Clicks on the dense original spectrum retain the exact requested frequency while the projected Gaussian reconstruction selects the nearest solved frequency; clicks on the discrete reconstruction snap both rows to that mode. The right column switches between U/V phase-HSV images with a shared magnitude scale, and all power/image comparisons use the same observation-topology candidate pixels. Existing caches, modal manifests, solver outputs, checkpoints, and visualization commands are unchanged.
- `flow3d/vis/viewer.py`: replaced the combined modal-phase component dropdown with a discrete mode-index slider, a read-only selected-frequency display, and a separate projected `u`/`v` dropdown. The integer slider snaps exclusively to frequencies present in the checkpoint while preserving the renderer's existing `(mode_index, component_index)` interface and all checkpoint/data contracts.
- `flow3d/scene_model.py`: made zero-mode modal placeholder buffers canonicalize to the current foreground Gaussian dimension when loading checkpoints and made foreground densification/culling remap every Gaussian-indexed modal buffer, including empty phi, observation-count, and trainable-mask tensors. This allows static checkpoints saved after Gaussian control to resume without weakening non-empty modal validation or changing checkpoint, optimizer, trajectory, or rendering contracts.
- `preproc/mask_app.py` and `preproc/mask_utils.py`: changed interactive XMem mask tracking to stream RGB frames from disk and immediately save each tracked foreground mask as a single-channel PNG in the selected sequence mask directory. The app no longer retains full video/image/mask preview lists, builds `tracked_colors.mp4`, or requires a separate Save Masks action; forward/backward progress is logged every 25 saved frames. Existing CLI arguments, SAM prompting, output names/directories, and the background-zero/foreground-nonzero mask contract remain unchanged.
- `preproc/extract_fixed_camera_video_frames.py` and `preproc/prepare_joint_colmap_video_dataset.py`: added a `noautorotate` fixed-camera orientation mode that ignores MOV display-rotation metadata without transposing encoded pixels. This supports corn static videos encoded as landscape `3840x2160` with `rotation=-90`, allowing a subsequent `0.5` scale to produce `1920x1080`; existing autorotate and explicit clockwise/counterclockwise modes, sweep extraction, output formats, and camera contracts remain unchanged.
- `preproc/extract_fixed_camera_video_frames.py` and `preproc/prepare_joint_colmap_video_dataset.py`: added explicit, shared fixed-camera rotation and uniform-scale controls for both COLMAP reference extraction and complete fixed-camera frame export. Manual clockwise/counterclockwise modes disable FFmpeg autorotation, apply rotation before Lanczos scaling, and record the transform in existing metadata; sweep extraction remains unchanged. Existing defaults, camera grouping, pose exports, file names, and format versions remain unchanged.
- `flow3d/trainer.py`: removed the full-frame total-alpha-to-one coverage term when an explicit background Gaussian set is present; mask supervision now retains only foreground/background separation, while foreground-only silhouette supervision, RGB loss, density control, checkpoints, and all data contracts remain unchanged.
- `preproc/extract_fixed_camera_video_frames.py` and `preproc/prepare_joint_colmap_video_dataset.py`: added fail-fast, atomic fixed-camera RGB sequence extraction from a configurable trim point and FPS, with per-view frame-name sidecars and provenance metadata; the joint COLMAP builder can optionally invoke it after reconstruction while continuing to send only one reference per fixed camera into COLMAP. Existing COLMAP, camera, scene-normalization, and sweep-dataset contracts remain unchanged, and the standalone extractor can backfill an already completed reconstruction without rerunning COLMAP.
- `preproc/prepare_joint_colmap_video_dataset.py`: fixed repeated reference extraction into the shared `static_refs` camera folder by allowing the already-created directory while retaining no-overwrite behavior for each labeled reference image; this preserves the intended two-camera grouping and all output contracts. Added a two-reference same-directory regression check; no FFmpeg or COLMAP reconstruction was run locally.
- `preproc/prepare_joint_colmap_video_dataset.py`: added a video-only joint-COLMAP dataset builder that extracts a sampled portrait sweep plus timestamp-selected landscape static references with FFmpeg, assigns one camera to all sweep frames and one shared camera to all static references, runs exhaustive COLMAP reconstruction, requires all references and a configurable sweep fraction to register in one model, exports raw and normalized camera manifests, writes the static-training `scene_norm_dict.pth`, and packages only registered sweep images with the selected sparse model. Existing camera, COLMAP binary, dataset, and training contracts are unchanged; no depth or placeholder masks are generated. Verification used Ruff, CLI help, pure transformation/parser and two-camera grouping checks, COLMAP command-construction checks, and `git diff --check`; FFmpeg and COLMAP reconstruction were not run locally.
- removed run_modal_surface.py, the only entry point left is modal_surface/__main__.py
- `run_modal_peak_pick.py`, `modal_peak_pick/apps/analyze_compensated_cache.py`, and `modal_peak_pick/core/background_compensation.py`: added an immutable `analyze-compensated` route for raw videos that excludes dilated per-frame foreground masks, estimates direct-to-reference background motion with LK/RANSAC, uses similarity by default to avoid anisotropic stretch, maps every frame once into the existing stabilized reference grid, and writes standard version-1 Farneback/FFT caches plus strict transform, coverage, validity, residual-background, and foreground contamination diagnostics.
- `modal_peak_pick/core/flow.py`, `flow3d/modal_flow_ab_comparison.py`, and `run_modal_flow_ab_comparison.py`: factored reusable pairwise dense flow and added stabilized-versus-raw-compensated A/B diagnostics over the exact existing manifest candidate pixels, including background residuals, contamination ratios, strong-motion retention, low/modal-band energy, regional flow comparisons, and plots. Frequency selection and SPOD cache contracts remain unchanged; no 3D solver, motion-fill, coordinate, phi, canonical, Viser, toy, or unit-test code was changed, and no tests or compilation were run per instruction.
- `run_training.py`, `flow3d/trainer.py`, and `flow3d/modal_canonical_optimization.py`: added an independent `canonical_only` modal route that keeps per-frame coordinates, staged phi, frequencies, cameras, and background fixed while optimizing only the five foreground canonical Gaussian tensors with valid-pixel RGB L1; modal Gaussian densification, duplication, culling, and opacity reset remain disabled.
- `run_modal_reconstruction.py` and `flow3d/renderer.py`: accepted canonical-only checkpoints in reconstruction and Viser, made modal overlays follow the refined canonical centers, verified every non-canonical model tensor remains bitwise unchanged from `init.ckpt`, and added foreground mean/RGB/opacity/scale/quaternion change diagnostics. No unit tests or compilation were run per instruction; verification was limited to static review and `git diff --check`.
- `modal_surface/carrier.py`: made Gaussian point observation construction pixel-candidates-only, removed Gaussian-center selection/z-buffer branches and metadata, and kept the separate carrier-point hard z-buffer workflow.
- `modal_surface/apps/solve_gaussian_modes.py`: removed Gaussian observation sampling and z-buffer CLI controls; Gaussian solves now always render depth/alpha and build pixel-candidate observations.
- `tests/test_modal_surface_cli.py`: updated Gaussian CLI coverage for the pixel-candidates-only command surface and the removed observation controls.
- `modal_surface/apps/_shared.py`, `solve_gaussian_modes.py`, and `solve_carrier_modes.py`: reused one open observation NPZ and one open latent NPZ per mode for diagnostic and manifest generation, eliminating repeated diagnostic file loads without changing output contracts.
- `modal_surface/carrier.py`: removed local-SNR estimation and per-view reliability weighting; observation confidence now uses only depth, contribution, and carrier z-buffer factors while frequency matching remains unchanged.
- `modal_surface/apps/_shared.py`, `match_carrier_views.py`, `solve_carrier_modes.py`, and `solve_gaussian_modes.py`: removed SNR-weighting CLI controls, builder arguments, diagnostics, and manifest fields.
- `tests/test_modal_surface_cli.py`: added coverage that the removed SNR-weighting options are unavailable on all observation commands.
- `modal_surface/carrier.py`: removed inverse-depth observation weighting and its reference-depth metadata while retaining rendered-depth unprojection, camera-depth diagnostics, and carrier z-buffer visibility.
- `modal_surface/apps/match_carrier_views.py`, `solve_carrier_modes.py`, and `solve_gaussian_modes.py`: removed depth-weighting CLI controls, builder arguments, and manifest parameters.
- `tests/test_modal_surface_cli.py`: added coverage that the removed depth-weighting options are unavailable on all observation commands.
- `modal_surface/carrier.py` and `modal_surface/apps/solve_gaussian_modes.py`: removed modal-amplitude percentile filtering from pixel-candidate sampling and deleted its CLI, API, NPZ, and manifest parameter.
- `tests/test_modal_surface_cli.py`: updated Gaussian CLI coverage for the removed `--pixel-min-mode-amp-percentile` option.
- deleted `modal_surface/optimization_multi.py`; app and toy callers now invoke `optimize_multi_view_staged` directly with a `StagedSolverConfig` built by `modal_surface/solver_cli.py`.
- `tests/test_solver_cli.py`: replaced facade-forwarding coverage with direct staged configuration construction checks.
- `modal_surface/optimization_staged.py`: stopped retaining and returning redundant inverse-alpha `betas`; beta remains a local synchronization variable and solver outputs are unchanged.
- `modal_surface/carrier.py`, `modal_surface/apps/match_carrier_views.py`, and `modal_surface/apps/solve_carrier_modes.py`: deleted the VGGT carrier-point observation/solve route and its CLI commands; retained direct Gaussian pixel-candidate construction in the new `modal_surface/gaussian_observations.py` module.
- `flow3d/modal_utils.py`, `run_training.py`, `flow3d/trainer.py`, and `flow3d/scene_model.py`: removed carrier-to-Gaussian KNN interpolation, rebinding, periodic modal refresh, and activation transport; modal training now loads solved Gaussian fields directly with strict manifest, latent-field, Gaussian identity, and observation-count validation while retaining the consistency-only Stage 2 cache rebuild.
- `preproc/bind_modal_field_to_3dgs_checkpoint.py`, `run_rendering.py`, `preproc/run_vggt.py`, and `preproc/vis_latent_modal_field.py`: deleted the KNN checkpoint binder, made rendering consume the direct Gaussian binder diagnostic key, and removed stale carrier-solver wording while keeping optional VGGT point export and visualization.
- `tests/test_modal_surface_cli.py` and `tests/test_modal_utils.py`: updated the canonical CLI surface and added deterministic direct Gaussian modal-field loading/rejection coverage; tests were not executed per instruction.
- `preproc/synthetic_modal_visibility_sphere.py`: preserved the linear sphere trajectory and added a separately solved tilted-ellipse trajectory using `phi = major - i * minor_axis_ratio * minor`, with configurable ellipse geometry, same-schema artifacts, shared-phase complex recovery diagnostics, and exclusive `track_label` entries in the overlay manifest.
- `preproc/vis_latent_modal_field.py`, `tests/test_synthetic_modal_visibility_sphere.py`, and `tests/test_vis_latent_modal_field.py`: added strict optional track-manifest validation, a Viser `Track` dropdown with one-hot runtime selection and phase-color synchronization, and deterministic trajectory/selection coverage; generated toy outputs and tests were not executed locally.
- `preproc/synthetic_modal_visibility_sphere.py`: added configurable equal-angle camera arcs through `--num-views` (default 2), preserved the original endpoint cameras and phase span, generalized all view-sized observation/viewer arrays, and added solver-valid-view-count diagnostics plus a count-colored Linear/Tilted ellipse viewer manifest.
- `tests/test_synthetic_modal_visibility_sphere.py`: added deterministic coverage for multi-view camera geometry, alpha spacing, generalized observation/viewer arrays, valid-view-count diagnostics and colors, exact trajectory RMSE, and two-view diagnostic compatibility; tests were not executed locally.
- `modal_surface/motion_fill.py`: added a reusable spatial KNN graph and joint sparse nullspace-completion solver with fixed anchors, variable per-point nullity, explicit anchor-free handling, and real/imaginary LSMR convergence metadata.
- `preproc/toy_modal_motion_fill.py`: added a strict standalone three-view Linear/Tilted ellipse motion-fill experiment for `K={4,8,16}`, preserving staged baselines while writing separate graphs, filled latents, GT/observable/filled overlays, Viser manifests, class/connectivity/error diagnostics, and a cross-K sensitivity summary.
- `tests/test_toy_modal_motion_fill.py`: added deterministic core, graph, preflight, and two-track artifact-contract coverage for motion fill; tests were not executed locally.
- `modal_surface/motion_fill.py`: generalized nullspace completion to an explicit fourth excluded role; excluded Gaussians and incident edges no longer bridge or constrain active components, while their staged observable field is retained exactly. KNN construction now handles exact duplicate Gaussian centers deterministically with epsilon-bounded zero-distance edge weights.
- `modal_surface/gaussian_motion_fill.py` and `modal_surface/apps/solve_gaussian_modes.py`: added opt-in formal Gaussian motion fill with a required scene-unit distance cutoff, one shared KNN graph across modes, staged-state role mapping, usable-row/nullspace checks, in-place final-field/provenance updates, filled PLY colors, and graph/per-mode/manifest diagnostics. Repeated pixel rows remain unaggregated, and rows from alpha-unidentifiable views remain stored but do not constrain completion.
- `tests/test_gaussian_motion_fill.py`, `tests/test_toy_modal_motion_fill.py`, `tests/test_modal_surface_cli.py`, and `tests/test_modal_utils.py`: added deterministic coverage for four-role mapping, excluded-node quarantine, duplicate centers, repeated same-view rows, near-zero measurements, CLI validation, artifact preservation, and loading final filled `phi`; tests were not executed locally per instruction.
- `modal_surface/cli.py` and `modal_surface/apps/optimize_multi_view.py`: removed the unused standalone `optimize-multi-view` CLI adapter and simplified the dispatcher to register only `solve-gaussian-modes`; the staged solver function remains the shared implementation used directly by the formal Gaussian pipeline and toy experiment.
- `tests/test_modal_surface_cli.py`: removed standalone optimizer command coverage and retained parser, dispatch, removed-option, lazy-3DGS-import, and motion-fill coverage for the sole public command; tests were not executed locally per instruction.
- `modal_surface/__main__.py`, `modal_surface/cli.py`, and `modal_surface/apps/__init__.py`: merged the sole-command parser and dispatcher directly into the package entrypoint, deleted the redundant CLI module, and kept `python -m modal_surface solve-gaussian-modes ...` unchanged.
- `tests/test_modal_surface_cli.py`: redirected CLI surface tests to `modal_surface.__main__`; tests were not executed locally per instruction.
- `modal_surface/__main__.py`: removed the obsolete single-command subparser and runner indirection; Gaussian solver arguments now live directly on `python -m modal_surface`, which invokes the Gaussian runner directly.
- `tests/test_modal_surface_cli.py`: updated CLI coverage for the flat root parser and removed the obsolete command token, namespace field, and subparser assumptions; tests were not executed locally per instruction.
- `modal_surface/apps/_shared.py` and `modal_surface/apps/solve_gaussian_modes.py`: renamed helpers used across modules to public, descriptive names while retaining `_finite_percentile` as an internal helper; function behavior and artifact contracts are unchanged.
- `modal_surface/apps/_shared.py`: widened `json_float` to the `SupportsFloat` protocol so NumPy floating scalars type-check without per-call casts while preserving the same finite-float JSON conversion behavior.
- `modal_surface/io.py`, `modal_surface/apps/_shared.py`, and `modal_surface/apps/solve_gaussian_modes.py`: moved modal-frequency loading into the central I/O module, moved the remaining single-consumer CLI and manifest helpers into the Gaussian solver app, and deleted the obsolete shared-helper module without changing solver outputs or artifact formats.
- `modal_surface/gaussian_motion_fill.py`: separated staged effective observability from motion-fill completion by deriving a float64 numerical-exact nullspace from the positive-weight alpha-identifiable observation operator; weak but algebraically full-rank partial Gaussians are now quarantined as `full_rank_nonanchor`, while true partial and unobserved nullspaces retain strict observation-preservation checks. Staged nullspace fields remain unchanged, and only numerical-nullity provenance was added.
- `tests/test_gaussian_motion_fill.py`: replaced the obsolete stored-basis leakage expectation with a deterministic `0.008` weak-direction regression that verifies full-rank exclusion does not block exact partial or unobserved completion; tests were not executed locally per instruction.
- `flow3d/modal_utils.py`, `flow3d/renderer.py`, and `flow3d/vis/viewer.py`: added strict motion-fill role provenance loading and a mode-selectable Viser point overlay that colors fixed anchors blue, unresolved partial points orange, completed points green, unresolved unobserved points purple, and excluded points gray; manifests without role metadata retain the existing uniform-blue overlay.
- `tests/test_modal_utils.py`: added deterministic role classification, color mapping, malformed metadata, mixed-mode metadata, and legacy-manifest coverage; tests were not executed locally per instruction.
- `modal_surface/motion_fill.py`: batched the per-point KNN boundary-radius queries into one `cKDTree.query_ball_point` call while preserving self removal, complete boundary ties, deterministic distance/index ordering, duplicate-center behavior, and all graph/output contracts; removed the superseded commented implementations.
- `modal_surface/gaussian_observations.py`: merged the single-consumer `_write_gaussian_observation_graph` implementation directly into `build_gaussian_observation_graph`, removing the redundant parameter-forwarding layer while preserving its public API and observation NPZ contract.
- `modal_surface/gaussian_observations.py`: renamed the validated static 3DGS attributes and per-view render arrays to direct Gaussian/render terminology and removed the intermediate `pixel_candidate_inputs` tuple variable without changing pixel-candidate sampling behavior.
- `modal_surface/gaussian_observations.py` and `modal_surface/apps/solve_gaussian_modes.py`: removed the redundant per-view pixel sample cap and its CLI/API/NPZ/manifest fields; every valid stride-sampled pixel now proceeds to Gaussian candidate matching.
- `tests/test_modal_surface_cli.py`: updated CLI coverage to require that the removed `--pixel-max-samples-per-view` option is no longer registered; tests were not executed locally per instruction.
- `modal_surface/apps/solve_gaussian_modes.py`, `modal_surface/gaussian_observations.py`, and `modal_surface/motion_fill.py`: build one foreground-Gaussian `cKDTree` per formal solver run and reuse it for motion-fill Gaussian-neighbor queries plus every mode/view pixel-candidate query; standalone callers retain local tree construction when no shared tree is supplied.
- `modal_surface/gaussian_observations.py` and `modal_surface/apps/solve_gaussian_modes.py`: removed the observed-only point-remapping option and generic optional point-field plumbing; Gaussian observations now always preserve the full checkpoint order and write contiguous `gaussian_indices` directly while retaining the existing NPZ fields.
- `modal_surface/gaussian_observations.py` and `modal_surface/apps/solve_gaussian_modes.py`: replaced the single-use generic `extra_metadata` dictionary with an explicit `source_checkpoint` argument and direct `point_type`/checkpoint NPZ fields, preserving provenance contracts with clearer data flow.
- `modal_surface/gaussian_observations.py`: replaced the generic `obs_fields_out` dictionary expansion with explicit contribution/surface diagnostic keywords in `np.savez_compressed`, preserving field contracts while eliminating Pylance's possible-`allow_pickle` type ambiguity.
- `modal_surface/optimization_staged.py`, `modal_surface/apps/solve_gaussian_modes.py`, `preproc/synthetic_modal_visibility_sphere.py`, and `tests/test_staged_modal_solver.py`: changed staged optimization to consume an in-memory observation mapping; formal solves now load each observation NPZ once and reuse its arrays for sanity checks, optimization, and statistics, while synthetic/tests pass their existing dictionaries directly.
- `preproc/synthetic_modal_visibility_sphere.py` and `tests/test_staged_modal_solver.py`: removed stale `source_observations_path` arguments after staged optimization provenance-path removal, aligning every call with the current in-memory observation API.
- `modal_surface/gaussian_observations.py`, `modal_surface/optimization_staged.py`, `preproc/synthetic_modal_visibility_sphere.py`, and `tests/test_staged_modal_solver.py`: removed the duplicate `obs_confidence` array and standardized the raw per-row field as `obs_contribution_weight`; staged `obs_effective_weight` remains the multiplicity-adjusted solver weight.
- `modal_surface/optimization_staged.py`: stopped carrying the raw `obs_contribution_weight` through `PreparedObservations` and the solved latent NPZ; the observation graph still supplies it to compute the retained `obs_effective_weight` used by all solver stages.
- `modal_surface/optimization_staged.py` and `tests/test_staged_modal_solver.py`: unified zero-overlap and below-threshold alpha failures as `insufficient_overlap`, removed the redundant raw structural-component graph and `alpha_structural_component_index` latent field, and retained raw shared-point counts for diagnostics.
- `modal_surface/optimization_staged.py` and `tests/test_staged_modal_solver.py`: unified overlap-count and degenerate-constraint alpha failures as `insufficient_information`, removed the intermediate shared-count component graph and `alpha_shared_count_component_index` latent field, and retained raw/informative edge diagnostics.
- `modal_surface/optimization_staged.py`: reused the initial all-view alpha constraints when the solve candidate still contains every view, while preserving restricted-view constraint rebuilding after candidate exclusions.
- `modal_surface/optimization_staged.py` and `tests/test_staged_modal_solver.py`: replaced multi-component alpha terminology with a single reference-connected information graph, excluded unreachable views from solve constraints, renamed component refinement internals to candidate refinement, and replaced component-index latent fields with `alpha_reference_connected_mask`.
- `modal_surface/optimization_staged.py`: added an explicit fail-fast check that narrows the bounded-complex IRLS result from optional before accessing SciPy optimizer fields, without changing successful solve behavior.
- `modal_surface/optimization_staged.py`, `modal_surface/io.py`, and `modal_surface/optimization_visualization.py`: split staged solving into a pure in-memory `StagedSolveResult`, an atomic rich debug writer, explicit post-write alpha-failure enforcement, reusable residual computation, and one final-field visualization pass.
- `modal_surface/gaussian_motion_fill.py` and `modal_surface/apps/solve_gaussian_modes.py`: replaced path-based latent rewriting with in-memory `GaussianMotionFillResult` processing, added row-free per-mode diagnostics, and switched the formal runtime latent to the exact eight-field compact schema plus three optional motion-fill viewer fields while preserving manifest version 1 and adding `diagnostics_path`.
- `modal_surface/gaussian_observations.py`, `modal_surface/gaussian_motion_fill.py`, and `modal_surface/io.py`: centralized atomic compressed-NPZ replacement for formal observation, graph, diagnostics, latent, and staged-debug writes without changing their retained data contracts.
- `preproc/synthetic_modal_visibility_sphere.py`, `tests/test_staged_modal_solver.py`, `tests/test_gaussian_motion_fill.py`, and `tests/test_compact_gaussian_artifacts.py`: kept synthetic/toy rich artifacts, updated staged and motion-fill tests for in-memory results, and added compact-schema, row-scaling, runtime-loader, manifest, and failure-path coverage; tests and compilation were not executed locally per instruction.
- `flow3d/renderer.py` and `flow3d/vis/viewer.py`: removed the Viser Gaussian-center debug overlay, including its foreground filter, count/size controls, per-frame center extraction, point-cloud state, and internal viewer parameter; retained Gaussian rendering, render hiding, and modal role point overlays.
- `flow3d/modal_utils.py`, `flow3d/renderer.py`, and `flow3d/vis/viewer.py`: replaced expanded viewer role-color transport with compact per-mode class IDs and added default-enabled Anchor/Partial/Filled/Unobserved/Excluded filters; the modal point count now applies once to the deterministic original-index union of enabled roles, with mode changes reapplying the same filters.
- `tests/test_modal_utils.py`: added deterministic single-role, multi-role shared-limit, no-role, and available-count-cap coverage for modal role selection; tests were not executed locally per instruction.
- `modal_peak_pick/core/cache.py` and `modal_peak_pick/apps/analyze_cache.py`: added a strict immutable version-1 modal-analysis cache with self-contained time/frequency arrays, source provenance, stage timings, read-only memmap loading, temporary-directory validation, and atomic publication; the new analyze stage performs decode, flow, smoothing, and full FFT exactly once.
- `run_modal_peak_pick.py`, `modal_peak_pick/apps/inspect_modes.py`, `pick_ui.py`, `export_modes.py`, and `modal_peak_pick/core/pipeline.py`: made cache input mandatory for inspect, pick, and export, removed their video-analysis controls and the obsolete video facade, and made export evaluate selected frequencies only through exact DFT of cached time-domain flow while preserving the existing `modal_analysis.npz` schema.
- `tests/test_modal_peak_pick_cache.py`: added deterministic cache corruption/immutability/failure tests, analysis-stage call-count and provenance coverage, cached inspect/pick path checks, off-bin exact-DFT export/schema verification, and mandatory-cache CLI coverage; tests were not executed locally per instruction.
- `preproc/prepare_modal_dynamic_dataset.py`: added an immutable atomic multi-view RGB/mask dataset builder that follows stabilization sidecar order, enforces shared aspect ratio and exact image/mask coverage, resizes to a requested width, and writes validated version-1 frame-map/time and source metadata.
- `run_training.py`: made modal VGGT training RGB-only when all depth losses are zero, always disable track loading on that route, save a modal-only immutable `init.ckpt`, always save `last.ckpt` after normal completion, and log Gaussian/mode/view/activation/trainability initialization details without changing model or checkpoint schemas.
- `run_modal_reconstruction.py`: added deterministic fixed-phi checkpoint reconstruction grouped by modal view/local index, with bounded-memory frame loading, padded observed/rendered/error MP4s, strict dataset/camera/checkpoint alignment checks, and per-view/overall masked RGB plus activation metrics.
- `tests/test_prepare_modal_dynamic_dataset.py`, `tests/test_run_training_phase0.py`, and `tests/test_modal_reconstruction.py`: added Phase-0 dataset, modal training plumbing, checkpoint policy, three-view reconstruction, metric, padding, and failure-cleanup coverage; tests and compilation were not executed locally per instruction.
- `flow3d/params.py`, `flow3d/modal_utils.py`, `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, and `run_training.py`: replaced free per-frame modal activations with zero-initialized per-view complex harmonic amplitudes driven by strict frame-map times and fixed manifest frequencies, made Gaussian training opt-in and modal control steps unavailable, and removed the obsolete activation-smoothness and modal-consistency chains.
- `run_modal_reconstruction.py`, `run_rendering.py`, `flow3d/renderer.py`, and `flow3d/vis/viewer.py`: enforced the `per_view_harmonic_v1` checkpoint contract, added per-view amplitude/phase reconstruction diagnostics, ordered Viser cameras by frame-map view IDs, and renamed trained/manual modal playback choices without changing the manual oscillator or motion-fill role filters.
- `tests/test_modal_harmonic.py`, `tests/test_run_training_phase0.py`, and `tests/test_modal_reconstruction.py`: added formal harmonic math, time-map, zero-gradient, trainability, checkpoint incompatibility, reconstruction, and Viser configuration coverage; toy-demo tests were not changed, and tests or compilation were not executed locally per instruction.
- `flow3d/params.py`, `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, and `run_training.py`: added the Stage 3A `anchor_delta_phi_v1` model/checkpoint contract, imported and froze Stage 2 per-view harmonic activations, optimized only a zero-initialized anchor-masked complex `delta_phi`, and added normalized 2D modal, staged-prior, and anchor-graph spatial losses while keeping all Gaussian parameters, staged phi, frequencies, and activations fixed.
- `flow3d/modal_utils.py`: added strict formal Stage 3A constraint loading from existing fixed-anchor role metadata, pixel-candidate observations, identifiable solver alphas, multiplicity-adjusted contribution weights, and the saved motion-fill graph without changing any artifact formats.
- `run_modal_reconstruction.py` and `flow3d/renderer.py`: made offline reconstruction consume refined effective phi and report per-mode anchor delta diagnostics, while explicitly rejecting Stage 3A checkpoints in the deferred Viser path and preserving Stage 2 behavior; no unit tests or compilation were run per instruction.
- `flow3d/params.py`, `flow3d/modal_utils.py`, `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, and `run_training.py`: replaced constant per-view harmonic activations with zero-initialized ragged complex envelope knots, deterministic 0.5-second per-view interpolation layouts, frame-weighted magnitude and normalized first-difference smoothness losses, envelope-only trainability, and the `per_view_harmonic_envelope_v1` checkpoint/Stage-3 source contract while keeping staged phi, frequencies, and all Gaussian parameters fixed.
- `run_modal_reconstruction.py`, `flow3d/renderer.py`, and `preproc/bind_gaussian_modal_field_to_3dgs_checkpoint.py`: switched reconstruction and Viser timestep playback to the trained harmonic envelope, added version-2 reconstruction metrics plus exact knot and per-frame temporal NPZ diagnostics, and made constant-activation checkpoints fail fast without a legacy fallback; unit tests and compilation were not run per instruction.
- `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, `run_training.py`, `run_modal_reconstruction.py`, and `flow3d/renderer.py`: replaced linear complex-envelope interpolation with per-view cubic Hermite interpolation, added normalized second-difference curvature regularization and maximum slope-change diagnostics, and advanced the fail-fast training, Stage-3 source, reconstruction, and Viser contract to `per_view_harmonic_envelope_v2`/`cubic_hermite_complex` while preserving knot, frame-map, and reconstruction NPZ layouts; unit tests and compilation were not run per instruction.
- `flow3d/modal_utils.py`, `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, `run_training.py`, `run_modal_reconstruction.py`, and `flow3d/renderer.py`: replaced anchor-only Stage-3 shape refinement with the `role_delta_phi_v1` RGB-driven route over anchor, partial, and filled Gaussians; removed the 2D modal observation loader, loss, diagnostics, and CLI weight; kept unobserved/excluded deltas fixed at zero; retained optional role-balanced staged prior and trainable-edge spatial regularization; and added per-role offline reconstruction diagnostics without changing manifest, latent, graph, frame-map, or Stage-2B artifact formats. Unit tests and compilation were not run per instruction.
- `modal_surface/anchor_residual_diagnostics.py` and `preproc/diagnose_gaussian_anchor_residuals.py`: added a solve-free exact replay of the staged per-Gaussian weighted complex least-squares residual and its point-view mean identity, decomposing every residual into within-view pixel-mode dispersion and cross-view disagreement. The independent version-1 artifact records per-view means, weights, energies, SSE contributions, worst views, continuous residual fractions, low-energy flags, and source classes, and is written only after matching the saved `point_precompletion_residual` and anchor status.
- `preproc/vis_anchor_structure_graph.py`: added strict residual-decomposition loading plus deterministic filters and colors for source class, within fraction, total/within/cross residual, modal signal RMS, and worst contributing view. Added static decomposition, replay-failure, artifact, loader, identity, color, and CLI coverage in `tests/test_anchor_residual_diagnostics.py` and `tests/test_anchor_structure_graph_standalone_viewer.py`; tests were not executed locally per instruction.
- `flow3d/data/casual_dataset.py`, `flow3d/configs.py`, `flow3d/trainer.py`, `run_training.py`, and `run_modal_reconstruction.py`: changed Stage-3 `role_delta` supervision to same-view random temporal pairs over configured frame gaps, added pair-equal signed frame-difference Charbonnier loss with weak optional absolute RGB and fixed-zero mask weight, recorded the `temporal_rgb_v1` checkpoint objective, and added deterministic per-view/per-gap reconstruction summaries plus `temporal_pair_metrics.npz`; model state, frame-map, staged modal, motion-fill, and Stage-2B checkpoint formats remain unchanged. Unit tests and compilation were not run per instruction.
- `flow3d/modal_flow_coordinates.py` and `run_modal_flow_coordinates.py`: added an immutable offline solver that strictly combines full Farneback reference-flow caches with shared pixel-candidate topology and fixed Gaussian mode fields, solves candidate-weighted per-frame complex modal coordinates through one normalized float64 ridge factorization per view, fixes the reference/temporal-mean gauge, and writes validated coordinate, conditioning, flow-fit, spectral-mixing, and timing artifacts without multiplying by staged alpha.
- `flow3d/params.py`, `flow3d/modal_utils.py`, `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, `flow3d/data/casual_dataset.py`, `run_training.py`, `run_modal_reconstruction.py`, `flow3d/renderer.py`, and `flow3d/vis/viewer.py`: directly replaced the formal cubic-envelope and sequential shape-refinement routes with fixed `per_frame_flow_coordinates_v1` buffers, zero-training checkpoint materialization, direct timestep coordinate playback, version-3 reconstruction/coordinate diagnostics, and explicit rejection of old envelope/refinement checkpoints; staged phi, frequencies, Gaussian state, manual oscillator playback, and motion-fill role visualization remain fixed. Unit tests and compilation were not run per instruction.
- `run_training.py`: corrected Stage-1 static checkpoint validation for fixed flow-coordinate materialization by accepting the empty modal buffers that every static `SceneModel` registers, while still requiring the static trajectory ID and rejecting trainable modal state, non-empty modal motion buffers, or enabled synthetic playback.
- `run_training.py`: made flow-coordinate frame-time identity validation compare the float64 artifact time after quantization to the active frame-map dtype, eliminating false mismatches from float32 storage at later frames while retaining exact frame name, view, local-index, and representable-time checks with detailed mismatch reporting.
- `run_modal_reconstruction.py`: made coordinate frequency diagnostics use the validated float64 times from the canonical frame map instead of the checkpoint's float32 runtime time buffer, preventing uniform 30-FPS sampling from being falsely rejected by accumulated float32 quantization while leaving rendering timestep selection unchanged.
- `flow3d/modal_basis_diagnostics.py` and `run_modal_basis_diagnostics.py`: added an immutable K-current modal-capacity audit that compares same-real-rank randomized PCA, exact exported 2D modal, and candidate-weighted lifted `J phi` subspaces on reference-relative Farneback flow; it also reports full-mask coverage, standalone-mode and solver-style ridge fits, global-fit regional residuals, and forward-flow photometric warp quality without recomputing optical flow, 3D modes, or motion fill. No unit tests or compilation were run per instruction.
- `flow3d/modal_frequency_selection.py` and `run_modal_frequency_selection.py`: added an immutable CPU-only dense exact-DFT selector that streams candidate-ROI flow statistics, greedily chooses shared frequencies as inseparable complex real/imaginary pairs using equal-view macro R2, directly rechecks pooled/view/regional residuals, and writes validated diagnostics plus export-compatible K-prefix shortlists without materializing full frequency dictionaries or running any 3D solve. No unit tests or compilation were run per instruction.
- `flow3d/modal_spod_analysis.py` and `run_modal_spod_analysis.py`: added an immutable per-view Welch/FDD-SPOD diagnostic route over cached candidate-ROI flow using 512/256-frame Hann snapshot Gram systems, scalar two-view support, split/leave-one-out/nonoverlap/cross-resolution stability, within-view spatial-MAC frequency-band merging, explicit slow-sway grouping below 0.2 Hz, boundary-clipped peak exclusion, and validated JSON/NPZ/plot/mode-preview outputs without changing the exact-DFT selector or any solver-facing artifact. No unit tests or compilation were run per instruction.
- `flow3d/modal_bilateral_graph.py` and `run_modal_bilateral_graph.py`: added an immutable, training-independent foreground-Gaussian graph builder that spatially preselects canonical neighbors, ranks them by scale-normalized position and activated-RGB bilateral affinity, retains deterministic mutual top-K edges, and writes future-rigidity weights, connectivity diagnostics, edge-strength histograms, and three orthographic edge previews without changing the existing distance-only motion-fill graph or K15 baseline artifacts. Mask/silhouette supervision remains intentionally absent from this graph-only stage. No unit tests or compilation were run per instruction.
- `flow3d/modal_bilateral_graph_view.py` and `run_modal_bilateral_graph_view.py`: added a read-only camera-projection consumer for an existing bilateral graph, with a configurable minimum edge affinity, bounded deterministic edge sampling, strict calibrated-view/background-image alignment, and affinity-colored PNG output; graph topology, weights, artifacts, and training remain unchanged. No unit tests or compilation were run per instruction.
- `flow3d/modal_joint_optimization.py`, `flow3d/params.py`, `flow3d/modal_utils.py`, `flow3d/scene_model.py`, `flow3d/configs.py`, `flow3d/trainer.py`, and `run_training.py`: added an explicit joint flow-coordinate/modal-shape training route initialized from the fixed K15 baseline, with zero residual parameters for per-frame complex coordinates and shared complex phi, exact per-view coordinate mean gauge, per-mode phi phase/RMS gauge, role-and-graph-degree phi masking, foreground RGB, reference-flow, full bilateral edge-strain rigidity, coordinate prior/frequency-aware temporal regularization, and role-balanced staged-phi prior; canonical Gaussian geometry/appearance, cameras, frequencies, unobserved/excluded phi, and low-degree points remain frozen, and mask is used only as RGB/flow support rather than a silhouette loss. `run_modal_reconstruction.py` and `flow3d/renderer.py` now consume the refined fields, export initial/effective coordinate diagnostics and joint phi statistics, and keep timestep/manual-oscillator/phase/role Viser displays consistent with the refined checkpoint. No unit tests or compilation were run per instruction.
- `flow3d/params.py`, `flow3d/scene_model.py`, `flow3d/modal_joint_optimization.py`, `flow3d/configs.py`, `flow3d/trainer.py`, and `run_training.py`: added a distinct `phi_only` refinement route whose flow-derived per-frame coordinates remain immutable while only masked complex `delta_phi` is trainable. The objective keeps foreground RGB, reference-flow, and actual-frame bilateral edge strain, and adds equal-phase per-mode rigidity at fixed coordinate-derived probe amplitudes plus weak bilateral consistency on trainable-to-trainable `delta_phi` edges; structural mode slots are deterministically cycled per step for K15 tractability. Canonical Gaussians, cameras, frequencies, coordinates, unobserved/excluded phi, and low-degree phi remain frozen, and mask remains support-only. `run_modal_reconstruction.py` and `flow3d/renderer.py` now strictly recognize the phi-only checkpoint contract and use refined phi for reconstruction and manual-oscillator playback while preserving old joint checkpoint consumption. No unit tests or compilation were run per instruction.
- `run_modal_peak_pick.py`, `modal_peak_pick/apps/analyze_compensated_cache.py`, and `modal_peak_pick/core/video_io.py`: allowed raw-background-compensated modal analysis to consume an explicitly ordered unstabilized image directory instead of decoding the source MP4, eliminating frame-rate/decimation ambiguity while preserving the same compensation, Farneback, FFT, immutable cache, and diagnostic contracts. No unit tests or compilation were run per instruction.
- `flow3d/modal_flow_ab_comparison.py`: made stabilized-background residual evaluation automatically use the common transform model recorded by the compensated caches, preventing a homography-compensated experiment from being unfairly compared against similarity-only stabilized diagnostics. No unit tests or compilation were run per instruction.
- `flow3d/modal_physics_coordinates.py` and `run_modal_physics_coordinates.py`: added an immutable Stage-P0 post-fit that keeps the K15 Gaussian fields fixed while fitting each view/mode complex coordinate to a shared-frequency damped oscillator with regularized latent forcing, an exact per-view mean-zero gauge, actual-time finite differences, shared real/imaginary sparse factorization, assigned-band and force diagnostics, and before/after candidate-flow evaluation including strong-motion frames. The strict version-1 coordinate NPZ schema is preserved so the result can be materialized and rendered without a new runtime forward path.
- `flow3d/modal_flow_coordinates.py`, `run_training.py`, `run_modal_reconstruction.py`, and `flow3d/renderer.py`: added multi-coordinate flow re-evaluation and sidecar solver provenance, then allowed fixed physics-postfit coordinates to propagate their actual solver/settings through checkpoint, reconstruction, and Viser metadata while retaining compatibility with all original reference-flow ridge artifacts. No unit tests or compilation were run per instruction.
- `modal_surface/apps/solve_gaussian_modes.py`, `modal_surface/incremental_manifest.py`, and `modal_surface/gaussian_motion_fill.py`: added strict contiguous-prefix manifest extension through `--base-manifest`, so a K15 Gaussian modal result can be extended by solving only mode slots 15-19; reused modes are referenced by rebased absolute artifact paths, the saved motion-fill graph is loaded instead of rebuilt, and checkpoint/view/parameter/frequency plus pixel-candidate topology compatibility is enforced before publishing the combined K20 manifest. No unit tests or compilation were run per instruction.
- `flow3d/modal_utils.py` and `tests/test_modal_utils.py`: made downstream motion-fill display classification accept sequential rigid-fill points that were completed as finite-safe components and then frozen with the `fixed_anchor` role, keeping them displayed as anchors while retaining fail-fast rejection of completed excluded points; latent, manifest, coordinate, checkpoint, and rendering contracts are unchanged, and tests or compilation were not run per instruction.
- `modal_surface/anchor_structure_graph.py`, `solve_gaussian_modes.py`, `flow3d/modal_utils.py`, `flow3d/renderer.py`, and `flow3d/vis/viewer.py`: added the opt-in per-mode mutual-KNN full-rank anchor diagnostic graph with adaptive Lab/depth filtering, strict standalone artifacts and manifest validation, and a static deterministic Viser overlay without changing staged solve, compact latent, or motion-fill outputs; added CLI, graph, artifact, compatibility, and display-helper coverage in the corresponding tests, which were not executed locally per instruction.
- `preproc/vis_anchor_structure_graph.py`: added an isolated NumPy plus modern-Viser browser viewer that strictly loads existing anchor-graph manifests or NPZ artifacts and displays deterministic real line segments, anchor RGB points, component/depth/combined colors, edge caps, and isolated anchors without importing Nerfview, Torch, or the main renderer; added pure NumPy loader/helper/CLI coverage in `tests/test_anchor_structure_graph_standalone_viewer.py`, and tests were not executed locally per instruction.
- `preproc/export_static_gaussians_for_viser.py` and `preproc/vis_anchor_structure_graph.py`: added a strict two-environment static-checkpoint visualization route that exports activated foreground and optional background 3DGS parameters to an atomic version-1 NPZ sidecar, verifies checkpoint/count/index/anchor-position identity against every loaded graph, converts WXYZ quaternion scales to covariance matrices, and overlays true modern-Viser Gaussian splats with visibility and scale controls; extended standalone viewer tests for covariance, provenance, anchor mismatch, and CLI validation, without changing checkpoint, solver, graph, manifest, or main-Viewer contracts. Tests were not executed locally per instruction.
- `preproc/vis_anchor_structure_graph.py`: recentered the standalone viewer world origin at the joint anchor AABB center across loaded modes and applied the same viewer-only translation to foreground/background Gaussian splats, anchor points, graph edges, and isolated anchors; initial orbit targeting now uses the origin while artifact validation remains in unchanged source coordinates. Extended the standalone viewer test skeleton for deterministic center and shared-translation checks; tests were not executed locally per instruction.
- `preproc/vis_anchor_structure_graph.py`: added a `Gaussian opacity` slider that updates modern-Viser foreground and optional background splat opacities together from immutable checkpoint-derived source values, preventing cumulative rescaling while leaving visibility, scale, graph, sidecar, and checkpoint contracts unchanged. Extended the standalone viewer test skeleton for opacity scaling, source preservation, and invalid input rejection; tests were not executed locally per instruction.
- `modal_surface/gaussian_observations.py`, `modal_surface/observation_coverage.py`, and `preproc/diagnose_gaussian_observation_coverage.py`: extracted the exact pixel-to-Gaussian contribution scorer and added a solve-free static replay diagnostic over configurable candidate K values. The independent version-1 artifact records per-Gaussian preselection, positive-contribution, top-K selection, best-rank, view-count, and four-category coverage diagnostics, and is written only after exact K-baseline view/sample/per-view counts reproduce the reference observation artifact.
- `preproc/vis_anchor_structure_graph.py`: added strict observation-coverage artifact loading and an independent deterministic Viser point overlay with K, category, point-cap, and point-size controls; graph, Gaussian sidecar, checkpoint, indices, and static positions are cross-validated without changing anchor graph or solver artifacts. Added static test coverage in `tests/test_observation_coverage.py`, `tests/test_anchor_structure_graph_standalone_viewer.py`, and existing checkpoint-loader mocks; tests were not executed locally per instruction.
- `preproc/vis_anchor_structure_graph.py`: added strict staged-status validation and an independent bright-magenta `partial_unresolved` Gaussian-center overlay to the residual diagnostic controls, reusing existing version-1 artifacts, deterministic point caps, point sizing, and world centering without changing solver or artifact contracts; extended standalone loader/helper coverage, and tests were not executed locally per instruction.
- `modal_surface/observed_structure_graph.py` and `preproc/build_observed_gaussian_structure_graph.py`: added a solve-free version-1 color-depth mutual-KNN structure graph over every foreground Gaussian with a positive row in an existing observation artifact, preserving single-view nodes and isolated nodes while recording observation provenance and node/view counts; the formal modal solver, alpha synchronization, staged classifications, phi, and motion fill remain unchanged.
- `preproc/vis_anchor_structure_graph.py`: added strict direct loading through `--observed-graph-npz`, observed-node world centering, static-checkpoint identity validation, and an independent modern-Viser observed graph folder/scene while retaining the existing anchor manifest and direct-NPZ behavior. Added static core, standalone-builder, and Viewer coverage in `tests/test_observed_structure_graph.py`, `tests/test_build_observed_gaussian_structure_graph.py`, and `tests/test_observed_structure_graph_standalone_viewer.py`; tests and compilation were not executed locally per instruction.
- `modal_surface/apps/solve_gaussian_modes.py`, `flow3d/modal_utils.py`, `flow3d/renderer.py`, and `flow3d/vis/viewer.py`: removed the earlier post-solve anchor-graph CLI, artifact/manifest writer, loader, main-Viewer plumbing, GUI, and scene nodes so the formal modal solve, phi, latent, and motion-fill pipeline no longer knows about structure graphs. Consolidated the retained mutual-KNN Lab/rendered-depth algorithm into `modal_surface/observed_structure_graph.py`, moved shared checkpoint/render inputs into `modal_surface/checkpoint_render_inputs.py`, replaced the legacy standalone entry point with observed-only `preproc/vis_observed_structure_graph.py`, and migrated relevant graph, sidecar, coverage, residual, and partial diagnostics tests with strict observation-path, view-order, node-mask, checkpoint, and static-position identity checks; tests and compilation were not executed locally per instruction.
- `modal_surface/rigid_component_solver.py`, `modal_surface/apps/solve_gaussian_modes.py`, `modal_surface/solver_cli.py`, `modal_surface/checkpoint_render_inputs.py`, and `modal_surface/optimization_visualization.py`: added the opt-in `rigid-components` path that strictly reuses each observed graph's source observations, preserves alpha synchronization, solves one complex normalized infinitesimal-SE(3) twist per connected component with deterministic truncated SVD, verifies the persisted complex64 field's first-order rigidity, and writes compatible compact latents plus additive version-1 manifest/component diagnostics while leaving the default staged route unchanged.
- `modal_surface/observed_structure_graph.py`: promoted the version-1 observed graph loader and source cross-validation into the formal module, with strict topology, threshold, accepted color/depth weight, support, component, count, checkpoint, view, Gaussian-index, static-position, and positive-observation identity validation; the graph artifact schema is unchanged.
- `modal_surface/gaussian_motion_fill.py`: added the rigid-seed adapter that fixes every connected observed node, assigns every other foreground Gaussian an identity three-dimensional nullspace, reuses the existing full-Gaussian KNN LSMR completion, preserves seeds bitwise, and reports observed/unobserved completed plus seed-free unresolved targets.
- `preproc/vis_observed_structure_graph.py` and the rigid graph/solver/CLI/motion-fill tests: added optional multi-mode rigid-manifest visualization of canonical/deformed graphs, phase and scale, residual/rank/finite-amplitude-drift colors, rigid seeds, completed targets, and unresolved targets with strict artifact cross-validation; tests and compilation were not executed locally per instruction, and only AST/diff static checks were performed.
- `modal_surface/rigid_component_solver.py`, `modal_surface/apps/solve_gaussian_modes.py`, and `modal_surface/solver_cli.py`: split rigid-component first-order validation into a strict complex128 model check and a complex64 persistence check with an exact per-edge cast-error bound, retained persisted-field finite-amplitude drift, and added model/persisted/quantization diagnostics without changing compact latent, graph, observation, manifest-version, staged-solver, Viewer, or renderer contracts. Added a short-edge/large-translation regression in `tests/test_rigid_component_solver.py`; tests and compilation were not executed locally per instruction.
- `preproc/vis_observed_structure_graph.py`: added rigid-result auto-play controls that advance deformed graph, rigid-seed, and motion-fill geometry at the selected mode's physical frequency with adjustable speed and refresh FPS, while retaining manual phase control and all artifact contracts. Added deterministic phase-advance validation in `tests/test_observed_structure_graph_standalone_viewer.py`; tests and compilation were not executed locally per instruction.
- `preproc/vis_observed_structure_graph.py`: replaced per-phase removal/recreation of observed graph line and point handles with in-place Viser `points` updates, reserving rebuilds for topology, mode, color, sampling, and visibility changes and updating line/point sizes directly. Added fake-Viser handle-identity coverage in `tests/test_observed_structure_graph_standalone_viewer.py`; tests and compilation were not executed locally per instruction.
- `preproc/vis_observed_structure_graph.py`: added rigid-component anomaly coloring for insufficient valid views, low full-rank singular-value ratio, and high component motion RMS, with adjustable Viser thresholds and strict reuse of existing rigid diagnostics/compact latent fields. Extended `tests/test_observed_structure_graph_standalone_viewer.py` with deterministic metric, loading, threshold, and edge-color coverage; artifact contracts are unchanged, and tests/compilation were not executed locally per instruction.
- `modal_surface/rigid_component_solver.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: added a post-solve trusted rigid-seed gate that defaults to at least two valid views and a full-system singular ratio of `1e-3`, preserves every raw component solve for diagnostics, zeros quarantined motion without fill, and makes quarantined candidates free motion-fill targets when fill is enabled. Added explicit candidate/trusted/effective-target and component-rejection diagnostics plus manifest parameters while keeping compact latent and graph formats unchanged. `preproc/vis_observed_structure_graph.py` now strictly validates the gate and can overlay trusted seeds in cyan and quarantined candidates in orange; CLI, solver, pipeline, and standalone Viewer tests were extended, but tests/compilation were not executed locally per instruction.
- `modal_surface/rigid_component_solver.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: extended the rigid trusted-seed gate with default minimum component sizes of four nodes and three edges, recorded size rejection and thresholds in rigid diagnostics/manifests, and kept rejected components as free motion-fill targets while preserving graph and compact-latent formats. `preproc/vis_observed_structure_graph.py` now strictly cross-validates the size policy and reuses the existing quarantine overlay; tests and compilation were not executed locally per instruction.
- `modal_surface/observed_structure_graph.py` and `preproc/build_observed_gaussian_structure_graph.py`: moved the four-node/three-edge minimum from post-solve seed selection into version-2 observed-graph construction, removing every rejected component edge after color/depth acceptance while retaining its positively observed Gaussians as explicitly recorded isolated nodes. Added configurable build CLI thresholds, pruning masks/counts, and strict topology/count validation. `modal_surface/rigid_component_solver.py`, `modal_surface/solver_cli.py`, `modal_surface/apps/solve_gaussian_modes.py`, and `preproc/vis_observed_structure_graph.py` removed the duplicate size gate and now retain only valid-view/singular-ratio post-solve filtering; compact latent and motion-fill role contracts are unchanged. Tests and compilation were not executed locally per instruction.
- `modal_surface/motion_fill.py` and `modal_surface/gaussian_motion_fill.py`: added one joint grouped-affine KNN LSMR in which rejected single-view rigid components share an unknown normalized six-DoF infinitesimal-SE(3) twist, trusted component points remain fixed, and all other targets retain independent three-DoF variables. Anchor connectivity is augmented by rigid-group membership, so each selected component completes atomically while its original single-view twist is discarded; exact model-space first-order rigidity is validated on observed-graph edges. `modal_surface/apps/solve_gaussian_modes.py`, `modal_surface/solver_cli.py`, and `preproc/vis_observed_structure_graph.py` added strict grouped-fill policies, masks, completion/twist/rigidity diagnostics, constrained-variable compact roles, and a blue completed single-view rigid-fill overlay without changing compact latent fields or downstream renderer interfaces. Tests and compilation were not executed locally per instruction.
- `modal_surface/apps/solve_gaussian_modes.py`, `modal_surface/gaussian_motion_fill.py`, and `modal_surface/motion_fill.py`: added always-on wall-clock profiling for the rigid-component pipeline, including setup and shared KNN graph construction, per-mode observation/alpha/rigid-solve/seed/output totals, and detailed grouped motion-fill preparation, connectivity, sparse assembly, real/imaginary LSMR, reconstruction, and validation stages; successful runs now write the additive `time_profile.json` without changing existing latent, diagnostic, graph, or manifest contracts.
- `modal_surface/rigid_component_solver.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: extended trusted rigid-seed selection with an adjustable maximum component finite-amplitude drift gate defaulting to `2.0`, preserving raw rigid solutions while zeroing or fill-targeting rejected candidates and adding explicit rejection/threshold diagnostics and manifest provenance. `preproc/vis_observed_structure_graph.py` now strictly validates the gate and adds a threshold-controlled finite-drift anomaly edge color while reusing the existing quarantined-candidate overlay; compact latent, observation, and graph formats remain unchanged, and tests were not modified or executed per instruction.
- `modal_surface/motion_fill.py`, `modal_surface/gaussian_motion_fill.py`, and `modal_surface/apps/solve_gaussian_modes.py`: removed grouped rigid-fill's repeated component-by-point scans by building one ordered membership/count/offset table, reused vectorized star edges for connectivity, batched normalized rigid blocks, coefficient reconstruction, atomic completion, and model validation, and added flushed live progress with sparse matrix dimensions plus real/imaginary LSMR iterations. The KNN objective, solver tolerances, compact latent, diagnostics, graph, manifest, and downstream renderer contracts are unchanged; tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: added an explicit `single-view-components` rigid motion-fill stage that decomposes each rejected one-view rigid twist into stable non-radial observation directions plus weak/ray-dominated directions, fills only the latter through cross-component KNN edges connected to trusted rigid seeds, and leaves every ordinary Gaussian at zero so this component stage can be evaluated independently. The reduced real/imaginary LSMR records observable rank, fill nullity, ray-basis count, trusted/cross KNN support, post-fill residual, radial/tangent RMS, ray ratio, finite drift, connectivity, and timing while retaining the existing compact latent and manifest version. `preproc/vis_observed_structure_graph.py` now strictly validates the additive diagnostics, switches between raw-rigid and final component-only geometry, and colors partial completion, rank, nullity, trusted support, and ray ratio; the existing joint fill remains the default, and tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`: replaced SciPy LSMR's default component-only iteration cap of the sparse-system column count with an explicit `max(1000, 4 * column_count)` budget and reports that budget in real/imaginary progress logs, preventing small strict-tolerance partial-fill systems from terminating at stop code 7 without changing their objective, tolerances, CLI, or artifact contracts. Tests were not modified or executed per instruction.
- `modal_surface/apps/solve_gaussian_modes.py`: made rigid motion-fill timing profiles record their explicit stage and print joint-only layout/reconstruction timings only for `joint`, while `single-view-components` reports its combined validation/residual stage; unknown stages fail explicitly. Solver behavior and existing artifact fields and versions remain unchanged, with only the additive timing-stage field, and tests were not modified or executed per instruction.
- `preproc/vis_observed_structure_graph.py`: replaced the fixed allclose check between separately persisted complex64 partial-fill point motion and rigid twist with a conservative operand-scaled real/imaginary quantization bound that accounts for translation/rotation cancellation, while retaining fail-fast rejection with explicit error and bound diagnostics for discrepancies beyond rounding. Solver and artifact contracts are unchanged, and tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`: changed component-only partial fill so every selected single-view component retains its stable non-ray observable twist, while only trusted-connected groups receive KNN weak-direction coefficients; unconnected groups keep zero nullspace correction and remain incomplete rather than having their whole twist erased. Post-fill residual, ray-motion, finite-drift, and persisted rigid-twist diagnostics now cover all selected groups. `preproc/vis_observed_structure_graph.py` validates those twists for every selected group while preserving completion as trusted-KNN connectivity; CLI and artifact fields and versions are unchanged, and tests were not modified or executed per instruction.
- `preproc/vis_observed_structure_graph.py`: restored the legacy source-camera inspection workflow in the standalone observed-graph Viewer by strictly loading the rigid manifest's ordered view configs, drawing centered color-coded camera frustums at a graph-relative scale, and adding visibility, per-view go-to-camera, and orbit-center controls. Solver artifacts and Viewer CLI remain unchanged, and tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: added a component-atomic post-fill gate to the single-view partial rigid stage. Candidate finite-amplitude drift above the existing rigid threshold or point-motion RMS divided by component radius above the new default `2.0` threshold zeros the complete component motion and twist, removes it from completion/anchor connectivity, and preserves pre-zero candidate metrics plus explicit rejection provenance. `preproc/vis_observed_structure_graph.py` strictly validates rejected components and adds a categorized post-fill rejection edge color; compact latent fields and manifest version remain unchanged, and tests were not modified or executed per instruction.
- `modal_surface/io.py`: moved OpenCV imports from module initialization into the non-NPY branches of depth and mask loading, allowing the isolated standalone observed-graph Viewer to reuse JSON-only `load_view_config` camera parsing without installing OpenCV. View-config, depth, mask, solver, artifact, and Viewer command contracts are unchanged; tests were not modified or executed per instruction.
- `modal_surface/rigid_component_solver.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: replaced raw view presence in trusted rigid-seed selection with a default one-third distinct-node coverage rule. Each component now records per-view distinct valid-node counts, its dominant view, secondary/dominant coverage ratio, and supported-view count; a view supports the component only when its node count is at least one third of the dominant count. `modal_surface/gaussian_motion_fill.py` treats components with one supported view as effective single-view groups while retaining sparse secondary observations in the SVD. `preproc/vis_observed_structure_graph.py` strictly validates the additive manifest/diagnostic fields, uses supported views for anomaly/fill classification, and adds raw count, supported count, and secondary-ratio edge colors. Compact latent, graph, observation, manifest-version, and downstream renderer contracts are unchanged; tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: removed the single-view partial-fill point-motion-RMS/component-radius rejection rule and its CLI, manifest, and diagnostic fields, leaving finite-amplitude drift as the sole component-atomic post-fill rejection gate. `preproc/vis_observed_structure_graph.py` now shows retained components in blue and finite-drift rejects in red, while accepting legacy artifacts whose retained masks include the removed rejection rule. Compact latent fields, manifest version, KNN completion, and downstream renderer contracts are unchanged; tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`, `modal_surface/motion_fill.py`, `modal_surface/solver_cli.py`, and `modal_surface/apps/solve_gaussian_modes.py`: replaced the rigid pipeline's simultaneous grouped-component/point solve with a default sequential pipeline. It first runs the existing single-view partial component fill, freezes only trusted seeds and finite-safe trusted-connected component completions, resets every other Gaussian to zero, and then assigns each remaining point an independent identity-basis three-dimensional variable in the ordinary pointwise LSMR. The obsolete grouped-affine joint solver and CLI choice were removed, component-only evaluation remains available, and detailed component/pointwise timing is recorded. `preproc/vis_observed_structure_graph.py` validates the sequential roles and final field, allows rejected or incomplete components to move independently after pointwise fill, retains partial diagnostics/colors, and keeps legacy joint artifacts read-only. Compact latent fields, observation/graph artifacts, manifest version, and downstream renderer interfaces are unchanged; tests were not modified or executed per instruction.
- `modal_surface/motion_fill.py`, `modal_surface/gaussian_motion_fill.py`, `modal_surface/apps/solve_gaussian_modes.py`, and `modal_surface/solver_cli.py`: added the single default `--motion-fill-max-anchor-hops 8` control to sequential rigid pointwise fill. Full-graph multi-source BFS distances remain diagnostic, while only anchor-connected points and edges at or below the hop limit enter LSMR; farther connected targets stay zero and incomplete. Per-mode diagnostics, profiles, and manifests record the threshold and truncated-target count. `preproc/vis_observed_structure_graph.py` strictly validates the hop-limited completion rule and explicitly retains read-only compatibility with earlier sequential artifacts that predate the threshold. Compact latent fields, graph/observation formats, manifest version, component partial fill, and downstream renderer interfaces are unchanged; tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py` and `modal_surface/motion_fill.py`: relaxed the shared real/imaginary LSMR absolute and relative stopping tolerances from `1e-10` to `1e-6` for both single-view component partial fill and sequential pointwise Gaussian completion, while retaining `conlim=1e8`, the same sparse objectives, diagnostics fields, artifact formats, and CLI surface. Tests were not modified or executed per instruction.
- `modal_surface/gaussian_motion_fill.py`, `modal_surface/apps/solve_gaussian_modes.py`, and `modal_surface/solver_cli.py`: changed sequential rigid fill to preserve every finite-drift-safe single-view component as a fixed downstream anchor, including trusted-KNN-unconnected components that retain only their stable non-ray observation twist. Trusted-KNN connectivity now controls only weak/ray-direction completion, while the eight-hop limit applies solely to remaining independent Gaussian targets. Added an explicit component anchor mask/policy and anchor-versus-connected counts. `preproc/vis_observed_structure_graph.py` validates the new policy, reconstructs every anchored component from its persisted twist, colors and overlays actual finite-safe anchors, and retains explicit read-only compatibility with earlier sequential artifacts. Compact latent, graph/observation formats, manifest version, and downstream renderer interfaces are unchanged; tests were not modified or executed per instruction.
- Integrated the final `rgbOptTest` observed-structure/rigid-component solver and sequential rigid motion-fill pipeline into the `modal718-rigid-fill-compat` flow-coordinate/physics/reconstruction mainline. `modal_surface/apps/solve_gaussian_modes.py` now preserves staged contiguous-prefix `--base-manifest` extension, supports direct rigid-component solves, and fails fast on the unsupported rigid-plus-base-manifest combination; `modal_surface/gaussian_motion_fill.py` retains both staged nullspace fill and finite-safe rigid-component anchor fill. Manifest version 1, compact latent fields, coordinate NPZ version 1, checkpoint parameterization, renderer behavior, and physics provenance remain unchanged. Added the rigid graph builders, diagnostics, standalone Viewer, and their existing test coverage; tests and compilation were not run per instruction.
- `modal_surface/observed_structure_graph.py` and `preproc/build_observed_gaussian_structure_graph.py`: upgraded observed graphs to version 3 by removing mode/frequency ownership while retaining the version-2 component-pruned topology and recording one topology-source observation for provenance. `modal_surface/solver_cli.py` and `modal_surface/apps/solve_gaussian_modes.py` now consume one shared graph plus explicit per-mode observation NPZs, copy the graph once, and publish manifest version 1 with `shared_observation_topology` and graph count 1. `modal_surface/manifest_combine.py` unifies split-run modes onto one shared graph path, and `preproc/vis_observed_structure_graph.py` loads one graph while switching per-mode rigid results. Observation, compact latent, motion-fill, coordinate, checkpoint, and reconstruction formats are unchanged; tests were not modified or executed per instruction.
- `modal_surface/apps/solve_gaussian_modes.py`: changed rigid-component runs to reference shared per-mode observations and the shared version-3 observed graph directly instead of copying them into every output, and added `--motion-fill-graph` for strict reuse of an external foreground-Gaussian KNN graph. `preproc/build_gaussian_observations.py` and `preproc/build_gaussian_motion_fill_graph.py` expose the existing observation and motion-fill graph builders as solve-free preprocessing commands. Observation, observed-graph, motion-fill-graph, latent, and manifest formats remain unchanged; tests were not modified or executed per instruction.
- `modal_surface/gaussian_observations.py` and `preproc/build_gaussian_observations.py`: replaced the reusable rigid observation bank's full per-mode NPZs with one version-1 shared topology plus lightweight per-mode sample measurements. Pixel-to-Gaussian candidate matching, Jacobians, contribution weights, and repeated row diagnostics are persisted once; each mode stores only frequency/provenance and one complex displacement per unique sampled pixel, with strict topology IDs and in-memory expansion preserving the existing solver observation mapping.
- `modal_surface/solver_cli.py`, `modal_surface/apps/solve_gaussian_modes.py`, and `modal_surface/manifest_combine.py`: rigid solves now consume one explicit observation topology and repeated measurement paths, load the topology once, publish manifest version 1 with `shared_topology_sample_measurements_v1`, and combine split runs without duplicating topology reads. Historical full-observation manifests remain read-only compatible in the combiner.
- `flow3d/modal_flow_coordinates.py`, `flow3d/modal_basis_diagnostics.py`, `preproc/build_observed_gaussian_structure_graph.py`, and `preproc/diagnose_gaussian_observation_coverage.py`: updated downstream topology and modal-value consumers for the split observation bank while retaining historical manifest loading. Observed-graph version 3, compact latent fields, coordinate/physics/checkpoint/reconstruction formats, and solver math are unchanged; tests were not modified or executed per instruction, and verification was limited to static review and `git diff --check` because repository linters were unavailable.
- `run.sbatch`: replaced the obsolete bush4 static-training command with one fail-fast K60 rigid modal-field job that validates the shared checkpoint, three-view basis, split observation bank, version-3 observed graph, and motion-fill graph, then sequentially runs otherwise identical `phase` and gain-enabled `bounded-complex` alpha variants into separate modal-field directories and logs. The existing Slurm resource directives and all rigid/motion-fill baseline parameters are retained; solver, manifest, NPZ, graph, coordinate, physics, checkpoint, and reconstruction contracts are unchanged.
- `modal_surface/rigid_component_solver.py` and `modal_surface/apps/solve_gaussian_modes.py`: changed alpha-excluded rigid components with zero usable observation rows from a fatal error into explicit zero-motion, zero-view, rank-zero non-seeds that remain eligible for sequential pointwise motion fill, and added their count to runtime and mode statistics. `preproc/vis_observed_structure_graph.py` now validates zero-view components without division by zero. `run.sbatch` skips a variant with a completed final manifest while still rejecting incomplete existing output, allowing a completed phase run to be retained when rerunning a cleaned bounded-complex variant. Manifest, compact latent, observation, graph, coordinate, physics, checkpoint, and reconstruction formats are unchanged; tests were not modified or executed per instruction.
- `run.sbatch`: removed the completed phase variant from the K60 Slurm job and made the script validate, skip, log, and run only the `bounded-complex` alpha model while retaining all shared inputs, rigid/motion-fill parameters, Slurm resources, output naming, and incomplete-output protection. Solver and artifact contracts are unchanged; tests were not modified or executed per instruction.
- `modal_surface/gaussian_observations.py`, `preproc/build_gaussian_observation_topology.py`, and `preproc/build_gaussian_observations.py`: separated mode-independent Gaussian observation topology construction from modal measurements. A standalone static-checkpoint/view-config command now writes the existing topology-v1 artifact, while the measurement command requires that topology and ordered modal NPZs; the former temporary first-mode observation route and its duplicate topology work were removed without changing topology, measurement, or composed full-observation formats.
- `run_modal_frequency_selection.py`, `flow3d/modal_frequency_selection.py`, and `flow3d/modal_flow_coordinates.py`: completely removed solved modal-manifest input from greedy frequency selection and made standalone observation topology the required ROI source. Frequency-selection v2 provenance now records topology identity, checkpoint, view configs, and sampling settings instead of baseline modes/frequencies/latents/alphas; exact-DFT, greedy selection, regional diagnostics, shortlist, and plotting math remain unchanged. Existing modal-manifest loading remains only in downstream flow-coordinate and diagnostic routes that actually consume solved 3D modes. No tests or compilation were run per instruction; verification used static review and `git diff --check`, and no configured linter was available.
- `modal_surface/gaussian_observations.py`: removed the unconsumed per-observation `obs_camera_z` array from newly built full observations, split topology, and topology identity hashing while retaining the transient all-Gaussian camera-depth calculation used to reject behind-camera candidates. Existing topology-v1 artifacts carrying the extra field remain readable, and topology, measurement, solver, graph, coordinate, checkpoint, and reconstruction versions are unchanged. Tests and compilation were not run per instruction.
- `modal_surface/gaussian_observations.py` and `modal_surface/apps/solve_gaussian_modes.py`: removed the redundant unnormalized `obs_contribution_score` array from newly built full observations, split topology, and topology identity hashing while retaining local raw scores for candidate ranking and normalized solver weights. Existing solve-statistics keys are preserved by reconstructing the raw score as `obs_contribution_weight * obs_contribution_sum`; existing topology-v1 artifacts carrying the extra field remain readable, and all solver math and artifact versions are unchanged. Tests and compilation were not run per instruction.
- `modal_surface/gaussian_observations.py`: removed the unconsumed duplicated surface-pixel coordinates, persisted rendered surface depth, and derived samples-per-view count from newly generated observation topology. Pixel/depth values remain local inputs to unprojection and candidate selection, legacy topology-v1 fields are ignored during composition, and topology identity no longer hashes the removed surface arrays. Solver, graph, frequency-selection, measurement, manifest, and downstream artifact versions are unchanged. Tests and compilation were not run per instruction.
- `modal_surface/gaussian_motion_fill.py`: made single-view partial rigid fill classify ray-dominated twist directions and report radial/tangent motion using only component members with positive-weight observations in alpha-identifiable views. Members observed only in alpha-excluded views now retain the shared component twist and downstream KNN participation without requiring a fabricated viewing ray, while components with no usable ray at all still fail explicitly. Alpha synchronization, rigid solving, motion-fill objectives, CLI, manifest, NPZ, and graph formats are unchanged; tests and compilation were not run per instruction, and verification used static review plus `git diff --check`.
- `modal_surface/apps/solve_gaussian_modes.py` and `run.sbatch`: added explicit rigid-component `--resume` support for interrupted K60 solves. A compact latent is the completion marker; matching successful diagnostics, observation identity, solver parameters, alpha model, rigid graph, and motion-fill graph are validated before the completed mode is skipped and its manifest entry is reconstructed. Modes without a latent are recomputed, the final manifest/diagnostics/time profile are still emitted normally, and the bounded-complex batch script now resumes an incomplete output directory instead of rejecting it. Existing NPZ and final manifest formats are unchanged; no tests or compilation were run, the unavailable Ruff command was noted, and verification used static review plus `git diff --check`.
- `modal_surface/optimization_staged.py`: replaced bounded-complex alpha profiling's per-constraint Python loop and repeated `np.linalg.lstsq` calls with one packed observation batch, precomputed per-point/per-view normal-equation sufficient statistics, batched 3x3 Hermitian eigensolves using the `lstsq(rcond=None)` singular-value scale, and vectorized observation/block residual evaluation. The original observation-space profiled objective, Huber IRLS, gain bounds, view exclusion, phase solver, diagnostics, CLI, and artifact formats are unchanged; no tests or compilation were run, and verification used static review plus `git diff --check`.
- `modal_surface/optimization_staged.py`: replaced bounded-complex profiling's capped frozen-weight Huber IRLS with one exact block-Huber residual transformation inside `least_squares`. This removes false alpha optimizer failures when a few block weights do not reach the strict 12-round fixed-point criterion, preserves the same profiled observation-space block-Huber cost, retains batched 3x3 point solves and gain/view policies, and records the truthful `profiled_exact_block_huber_gauss_newton` information kind without changing NPZ/manifest shapes. No tests or compilation were run; verification used static review plus `git diff --check`.
- `modal_surface/optimization_staged.py`: increased only the bounded-complex exact block-Huber `least_squares` evaluation budget from 100 to 500 so well-conditioned modes can converge instead of being excluded solely for exhausting the short optimizer budget. Phase optimization, convergence criteria, objective, initialization, gain bounds, view exclusion, CLI, diagnostics, manifests, and NPZ formats are unchanged; tests and compilation were not run per instruction.
- `flow3d/modal_flow_coordinates.py`: allowed completed rigid modal manifests to retain explicit alpha-unidentifiable mode/view entries when solving per-frame flow coordinates. The loader still requires ordered boolean identifiability and finite alpha diagnostics, while the unchanged coordinate design continues to use only the solved global `phi`, observation Jacobians, and full reference-flow caches without multiplying by alpha. Coordinate math, CLI, manifest, checkpoint, reconstruction, and NPZ formats are unchanged; tests and compilation were not run per instruction.
- `preproc/vis_observed_structure_graph.py`: removed the standalone isolated-observed-node visibility and point-size controls together with their redundant red point-cloud overlay and CLI size option. Isolated nodes remain available through the ordinary observed-node display, while graph artifacts and rigid-manifest contracts are unchanged; tests and compilation were not run per instruction.
- `preproc/vis_observed_structure_graph.py`: restricted graph edges and ordinary observed-node points to the selected mode's final sequential anchors when a rigid manifest is loaded: trusted rigid-solve seeds union finite-safe first-stage single-view component anchors. Filtering now precedes the visible-edge cap and excludes second-stage pointwise-fill targets; graph-only viewing without a rigid manifest and all artifact contracts remain unchanged, with no additional CLI change. Tests and compilation were not run per instruction.
- `flow3d/renderer.py`: distinguished empty static-checkpoint modal-coordinate placeholder tensors from populated modal coordinate state, allowing ordinary static 3DGS checkpoints to load in Viser while preserving strict rejection of incomplete, malformed, or unsupported modal checkpoints. Checkpoint, renderer, training, and visualization contracts are unchanged.
- `preproc/prepare_colmap_modal_view_configs.py`: added a dedicated COLMAP-only replacement for the former VGGT modal-geometry stage. It reads normalized fixed-reference cameras from the joint-COLMAP manifest, scales intrinsics to the modal resolution, validates and packages ordered ROI masks, renders matching foreground depth from the final static 3DGS checkpoint, and atomically writes the existing modal view-config/depth/mask contract with provenance and overlap diagnostics. No existing API, checkpoint, COLMAP, or modal artifact format was changed, and no new dependency was added.
- `preproc/prepare_colmap_modal_view_configs.py`: accepted ROI masks only when they already match the target modal resolution or exactly match the registered reference-camera source resolution; source-resolution masks are reduced with nearest-neighbor sampling and the transform is recorded in metadata, while every other size still fails explicitly. Camera, checkpoint, depth, and view-config contracts are unchanged.
- `flow3d/vis/viewer.py` and `flow3d/renderer.py`: added a render-only `Hide background` checkbox when a checkpoint contains background Gaussians. The viewer reuses the existing Gaussian filter-mask render path to exclude background splats while retaining foreground motion, manual modal playback, diagnostic colors, checkpoint state, and default rendering behavior.
- `run_experiment_pipeline.py` and `preproc/experiment_pipeline_template.yaml`: added one strict, resumable post-COLMAP experiment controller that chains static 3DGS, native-resolution image-sequence flow caches, COLMAP view configs, topology/frequency/modal artifacts, optional approved rigid-component graphs, rendered projection coordinates, physics post-fit, and direct/physics Viser-ready checkpoints. It pauses only for static quality and rigid-graph approval, records immutable receipts/provenance, never resizes after the canonical image/mask export, and excludes original-video reconstruction. `run_modal_peak_pick.py`, `modal_peak_pick/apps/analyze_cache.py`, `modal_peak_pick/core/flow.py`, `preproc/prepare_modal_dynamic_dataset.py`, `preproc/prepare_colmap_modal_view_configs.py`, and `flow3d/modal_flow_coordinates.py` now accept exact ordered native image sequences and the actual COLMAP-selected non-middle reference frame while preserving legacy video/target-width artifacts. `run_training.py` and `flow3d/trainer.py` now publish configs and checkpoints atomically so controller completion markers cannot mistake an interrupted final epoch for a finished static run. Added CPU-only controller test coverage in `tests/test_run_experiment_pipeline.py`; tests, compilation, and GPU rasterization were not run per instruction, and verification was limited to static review and `git diff --check`.
- `modal_surface/soft_elastic_solver.py`, `modal_surface/solver_cli.py`, `modal_surface/apps/solve_gaussian_modes.py`, and `run_experiment_pipeline.py`: added `soft-elastic` as a third experimental modal-field solver alongside staged and rigid-components. It reuses the approved color/depth observed graph as soft adjacency, estimates one complex 3-D displacement per observed Gaussian with matrix-free LSMR, first-order axial-stretch and weak weighted graph-Laplacian penalties normalized against the data information scale, retains the staged alpha synchronization and observation-preserving full-Gaussian motion fill, and supports strict per-mode resume plus solver diagnostics. Existing compact latent, version-1 modal manifest, rendered-design, coordinate, physics, checkpoint, renderer, and reconstruction contracts remain unchanged; legacy configs without `soft_elastic` retain their resolved payload. Added synthetic solver and controller-command coverage in `tests/test_soft_elastic_solver.py` and `tests/test_run_experiment_pipeline.py`; tests and compilation were not run per instruction, Ruff was unavailable, and verification used static review plus `git diff --check`.
